<% @page_title = "Patient List" %>

<h1>Listing patients</h1>

<table>
    <th>Mrn ampath</th>
<!--    
    <th>Mrn mtrh ip</th>
    <th>Mrn mtrh op</th>
    <th>National ID</th>
-->
    <th>Name</th>
    <th>Gender</th>
    <th>Race</th>
    <th>Tribe</th>
    <th>Address1</th>
    <th>Address2</th>
      
  </tr> 
<% for patient in @patients %>

  <tr>
    <td><%=h patient.send("mrn_ampath") %></td>
<!--    
    <td><%=h patient.send("mrn_mtrh_ip") %></td>
    <td><%=h patient.send("mrn_mtrh_op") %></td>
    <td><%=h patient.send("national_identifier") %></td>
-->
    <td><%=h patient.send("prefix") << ' ' << patient.send("given_name") << ' ' << patient.send("family_name") %></td>
    <td><%=h patient.send("gender") %></td>
    <td><%=h patient.send("race") %></td>
    <td><%=h patient.send("tribe") %></td>
    <td><%=h patient.send("address1") %></td>
    <td><%=h patient.send("address2") %></td>
    <td><%= link_to 'Show', :action => 'show', :id => patient %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => patient %></td>
    <td><%= link_to 'Destroy', { :action => 'destroy', :id => patient }, :confirm => 'Are you sure?', :post => true %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Previous page', { :page => @patient_pages.current.previous } if @patient_pages.current.previous %>
<%= link_to 'Next page', { :page => @patient_pages.current.next } if @patient_pages.current.next %> 

<br />

<%= link_to 'New patient', :action => 'new' %>