<% @encounter.images.each do |i|%>
    <div id="thumbnail-<%= i.id %>" class="thumbnail" >
		<%= link_to(image_tag("/image/thumb/#{i.id}.jpg"), :controller => "image", :action => "view", :id => i) %><br/>
    </div>
<% end %>